class BattleAction {

    constructor(monsterBattleId, skillId, targetBattleId, targetGroup) {
        this.monsterBattleId = monsterBattleId;
        this.skillId = skillId;
        this.targetBattleId = targetBattleId;
        this.targetGroup = targetGroup;
    }
}